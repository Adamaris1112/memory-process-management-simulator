package simulator;
import SimulatorGUI;

public class Main {
    public static void main(String[] args) {
        // Initialize the GUI for the memory process management simulator
        SimulatorGUI simulatorGUI = new SimulatorGUI();
        simulatorGUI.setVisible(true);
    }
}